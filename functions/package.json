{
  "name": "orderflow-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "typecheck": "tsc --noEmit",
    "seed:demo": "node scripts/seedDemoMerchant.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.2.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
